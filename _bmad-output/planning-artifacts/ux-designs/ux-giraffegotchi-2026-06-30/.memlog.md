---
topic: On-device animal/species selector for giraffegotchi (ESP32 CYD 320x240 landscape, resistive touch, 5 care buttons)
updated: 2026-06-30T22:44
---

- (assumption) Visual identity is INHERITED from existing firmware, not fresh: RGB565 palette, TFT_eSPI FONT2/FONT4, rounded-rect buttons (fillRoundRect r8 + white outline), 320x240 landscape, top meter row y8-24, bottom button row y198-236, giraffe box x85-235 y34-194. DESIGN.md ratifies this, does not invent a brand.
- (event) UX scoped to selector only per Elliot; keep-it-small; Fast path
- (decision) Entry gesture: long-press BOOK ~800ms opens the picker. Distinct from BOOK's existing tap=read and fast-mash=revive. [ASSUMPTION] picker is unavailable while the pet is 'dead' (prank state) — must revive first; keeps the dead-state gag intact.
- (decision) Picker is a MODAL full-screen tile grid. Each tile = the species' happy sprite (scaled) + lowercase name. Current animal highlighted (outline). A [back] tile cancels with no change. World keeps ticking while open (no decay pause — consistent with existing design where only night sleep pauses).
- (decision) Select: tap a tile -> atomic species swap per AD-13 -> persist active-species id to NVS -> full-screen repaint into the new animal AND its biome -> return to pet screen. A brief 'swapping...' beat is acceptable given the buffer realloc + repaint AD-13 requires.
- (decision) Grid capacity: single screen, up to ~10 tiles (5 across x 2 rows on 320x240). [ASSUMPTION] paging/scroll deferred until the roster exceeds one screen (bounded anyway by the AD-14 flash budget).
- (decision) Microcopy: title 'PICK YOUR PAL', tile labels are lowercase animal names, cancel is 'back'. Playful, matches the pet's tone. Kept ultra-short for the tiny screen.
- (decision) Accessibility floor: resistive single-touch; tiles >=44px targets (64px actual); high-contrast white text on dark tiles (inherits existing FONT2/FONT4 + white-on-dark). No audio cues (no sound subsystem yet).
- (assumption) Form factor: one embedded touchscreen, 320x240 landscape, resistive single-touch (no swipe/multitouch). No external UI system — custom TFT_eSPI primitives; DESIGN.md inherits the existing firmware's button/meter primitives.
- (event) spines finalized (Fast path, keep-small): DESIGN.md + EXPERIENCE.md for the animal selector; reviewer gate offered/skipped per scope
